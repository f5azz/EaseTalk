<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌙 Main App Theme -->
    <style name="Theme.Easetalk" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your main app colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:statusBarColor">@color/purple_700</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- 🚀 Splash Screen Theme -->
    <style name="Theme.Easetalk.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Fullscreen splash -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- Optional splash background image -->
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>
